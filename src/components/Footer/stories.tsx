// Packages
import { Story, Meta } from "@storybook/react/types-6-0"

// Components
import Footer from "."

export default {
  title: "Footer",
  component: Footer
} as Meta

export const Default: Story = () => <Footer />
