// Packages
import { Story, Meta } from "@storybook/react/types-6-0"

// Components
import TextContent from "."

export default {
  title: "TextContent",
  component: TextContent
} as Meta

export const Default: Story = () => <TextContent />
