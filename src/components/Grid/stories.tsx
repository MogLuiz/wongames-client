// Packages
import { Story, Meta } from "@storybook/react/types-6-0"

// Components
import Grid from "."

export default {
  title: "Grid",
  component: Grid
} as Meta

export const Default: Story = () => <Grid />
