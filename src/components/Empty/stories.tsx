// Packages
import { Story, Meta } from "@storybook/react/types-6-0"

// Components
import Empty from "."

export default {
  title: "Empty",
  component: Empty
} as Meta

export const Default: Story = () => <Empty />
